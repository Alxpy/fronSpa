export interface PLogin {
  email: string;
  password: string;
}

export interface PRegister {
  name: string;
  email: string;
  password: string;
  phone: string;
  document: string;
  address: string;
}
